import React from 'react';
import Export from '../Export/Export';
import Reset from './Reset';

function Footer() {
    return (
        <section>
            <Reset/>
            <Export/>
        </section>
    );
}

export default Footer;